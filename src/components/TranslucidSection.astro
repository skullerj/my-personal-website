---
export interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<section
  class={` max-w-3xl bg-cream/40 p-4 rounded-2xl backdrop-blur-sm  slide-in-left ${className}`}
>
  <slot />
</section>
