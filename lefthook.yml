# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#

pre-commit:
  parallel: true
  jobs:
    - run: npx biome format {staged_files} --write
      glob: "*.{js,ts,jsx,tsx,css,astro,json}"
